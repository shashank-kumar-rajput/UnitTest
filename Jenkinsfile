pipeline {
  agent any
    stages {
      stage("Compile") {
     steps {
      sh "Compiling done"
      }
 }
  stage("Unit test") {
      steps {
         sh "python testingpy.ut.py"
        }
     }
  }
}
